# ---------------------------
# 🪟 WINDOWS (tabs)
# ---------------------------
bind n new-window
bind x kill-window
bind , command-prompt -I "#W" "rename-window '%%'"

# Window switching — Alt+hl
bind -n M-h previous-window
bind -n M-l next-window
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5

# Enter resize mode
bind c switch-client -T resize

# Repeatable resizing (IMPORTANT: -r flag)
bind -r -T resize h resize-pane -L 5
bind -r -T resize j resize-pane -D 5
bind -r -T resize k resize-pane -U 5
bind -r -T resize l resize-pane -R 5

# Exit resize mode
bind -T resize Escape switch-client -T root


# ---------------------------
# 🔲 PANES
# ---------------------------
bind r split-window -h
bind d split-window -v
bind D detach-client

# Pane navigation — Alt+hjkl (window switching uses Alt+hl, pane swap uses jk)
# bind -n M-j swap-pane -D
# bind -n M-k swap-pane -U

# # Ctrl+l still clears terminal when not in nvim
# bind -n C-l if-shell "[ '#{pane_current_command}' = 'nvim' ]" \
#   "send-keys C-l" \
#   "send-keys C-l \; run 'sleep 0.1' \; clear-history"

# Ctrl+g L — clear terminal scrollback (prefix + L)
bind L send-keys "clear" Enter \; clear-history

# ---------------------------
# 🪟 FLOATING (bottom-right block)
# ---------------------------
bind -n M-p if-shell "[ $(tmux list-panes | wc -l) -eq 1 ]" \
  "split-window -v -p 33" \
  "resize-pane -Z"


# ---------------------------
# 🔁 RELOAD
# ---------------------------
bind R source-file ~/.config/tmux/tmux.conf \; display "Reloaded!"

# ---------------------------
# ⚙️  MISC
# ---------------------------
set-window-option -g mode-keys vi
set-option -g status-position bottom
