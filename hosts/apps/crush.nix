{
  programs.crush = {
    enable = true;
    # Optional: you can configure providers or settings here
    settings = {
      # Example placeholder
      # providers = { openai.api_key = "your-key"; };
    };
  };
}

