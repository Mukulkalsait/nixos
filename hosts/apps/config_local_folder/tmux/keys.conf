# ---------------------------
# 🪟 WINDOWS (tabs) — Alt+hl
# ---------------------------
bind n new-window


# Ctrl+Alt+q — kill entire tmux session instantly
bind -n C-M-q kill-session
# bind x kill-window


bind -n M-h previous-window
bind -n M-l next-window
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5

# ---------------------------
# 🔲 PANES — Ctrl+hjkl (vim-tmux-navigator owns these)
# ---------------------------
bind r split-window -h
bind d split-window -v

# DO NOT manually bind C-h/j/k/l here
# vim-tmux-navigator handles all of these

# ---------------------------
# 🔀 PANE SWAP (optional but useful)
# ---------------------------
# bind -n M-j swap-pane -D
# bind -n M-k swap-pane -U

# ---------------------------
# 🪟 FLOATING toggle
# ---------------------------
bind -n M-p if-shell "[[ $(tmux list-panes | wc -l) -eq 1 ]]" \
  "split-window -v -p 30" \
  "resize-pane -Z"

# ---------------------------
# 🔁 RELOAD
# ---------------------------
bind R source-file ~/.config/tmux/tmux.conf \; display "Reloaded!"

set-window-option -g mode-keys vi
set-option -g status-position top




# # ---------------------------
# # 🪟 WINDOWS (tabs)
# # ---------------------------
#
# bind n new-window
# bind x kill-window
#
# # ---------------------------
# # 🔲 PANES
# # ---------------------------
#
# bind r split-window -h
# bind d split-window -v
#
# # Pane navigation (clean, no conflict)
# bind -n M-h select-pane -L
# bind -n M-j select-pane -D
# bind -n M-k select-pane -U
# bind -n M-l select-pane -R
#
# # ---------------------------
# # 📐 LAYOUT
# # ---------------------------
#
# bind -n M-[ select-layout even-horizontal
# bind -n M-] select-layout even-vertical
#
# # ---------------------------
# # 🪟 FLOATING (toggle hack)
# # ---------------------------
#
# bind -n M-p if-shell "[[ $(tmux list-panes | wc -l) -eq 1 ]]" \
#   "split-window -v -p 30" \
#   "resize-pane -Z"
#
# # ---------------------------
# # 🔁 RELOAD
# # ---------------------------
#
# bind R source-file ~/.config/tmux/tmux.conf \; display "Reloaded!"
#
# set-window-option -g mode-keys vi
# set-option -g status-position top


